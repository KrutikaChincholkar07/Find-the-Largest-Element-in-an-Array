# Find-the-Largest-Element-in-an-Array
1 Assume the first element is the largest. 2 Compare it with every other element. 3 If you find a bigger number → update the largest value. 4 After scanning all elements → the final value is the maximum number.
